{
  "name": "lostsocks-api",
  "version": "1.0.0",
  "description": "API пародийного каталога носков без пары",
  "type": "module",
  "main": "server.js",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node --env-file-if-exists=../.env server.js",
    "dev": "node --env-file-if-exists=../.env --watch server.js"
  },
  "dependencies": {
    "express": "^5.1.0",
    "multer": "^2.4.0"
  }
}
